package com.example.attendanceapplication.activities;

import android.os.Bundle;
import android.view.MenuItem;
import androidx.appcompat.app.AppCompatActivity;
import androidx.appcompat.widget.Toolbar;
import com.example.attendanceapplication.R;

/**
 * SessionManagementActivity – teacher manages active attendance sessions.
 * Full implementation already in SessionManagementActivity.java (the QR screen).
 */
public class SessionManagementActivityPlaceholder extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }
}
