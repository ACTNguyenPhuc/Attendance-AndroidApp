<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <application
        android:name=".AttendanceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AttendanceApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Teacher Activities -->
        <activity android:name=".activities.TeacherMainActivity"
            android:exported="false" />
        <activity android:name=".activities.CreateClassActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.ClassDetailTeacherActivity"
            android:exported="false" />
        <activity android:name=".activities.SessionManagementActivity"
            android:exported="false" />
        <activity android:name=".activities.ShiftAttendanceListActivity"
            android:exported="false" />
        <activity android:name=".activities.CreateSessionActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Student Activities -->
        <activity android:name=".activities.StudentMainActivity"
            android:exported="false" />
        <activity android:name=".activities.JoinClassActivity"
            android:exported="false" />
        <activity android:name=".activities.ScanAttendanceActivity"
            android:exported="false" />
        <activity android:name=".activities.AttendanceResultActivity"
            android:exported="false" />
        <activity android:name=".activities.ClassDetailStudentActivity"
            android:exported="false" />

        <activity android:name=".activities.ShiftDetailActivity"
            android:exported="false" />

        <!-- Shared Activities -->
        <activity android:name=".activities.ProfileActivity"
            android:exported="false" />

        <!-- Firebase Cloud Messaging -->
        <service
            android:name=".utils.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
